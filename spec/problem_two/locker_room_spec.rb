require 'rspec/given'

require './lib/problem_two/locker_room'


describe "the locker room" do
  describe "when retrieving a bag with a ticket" do
    context "and the bag is there" do
      # Given (:location) { ProblemTwo::LockerRoom.store  }
      # Given (:ticket) { mock "ticket", :location => location }
      # Given { ticket.should_receive(:invalidate).once }
      # When (:result) { ProblemTwo::LockerRoom.retrieve ticket }
      # Then { result.should be_true }
    end
  end

  
end
